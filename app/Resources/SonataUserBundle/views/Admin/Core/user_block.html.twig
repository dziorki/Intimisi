{% block user_block %}
    {% if app.user %}
        {{ app.user }}

        {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
            <a href="{{ url('sonata_user_impersonating', {'_switch_user': '_exit'}) }}">(exit)</a>
        {% endif %}

        - <a href="{{ url('logout') }}">{{ 'user_block_logout'|trans({}, 'SonataUserBundle') }}</a>
    {% endif %}
{% endblock %}
