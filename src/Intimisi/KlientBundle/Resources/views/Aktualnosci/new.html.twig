{% extends "::base.html.twig" %} 
{% block content %}

<div class="title"><h5>Aktualności</h5></div>

<form action="{{ path('aktualnosci_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('aktualnosci') }}">
            Back to the list
        </a>
    </li>
</ul>
<div class="fix"></div>
{% endblock %}