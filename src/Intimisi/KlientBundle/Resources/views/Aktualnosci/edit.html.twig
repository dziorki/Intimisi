{% extends "::base.html.twig" %} 
{% block content %}

<div class="title"><h5>Aktualności</h5></div>

<form action="{{ path('aktualnosci_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('aktualnosci') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('aktualnosci_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
<div class="fix"></div>
{% endblock %}